{
  "name": "create-repo",
  "version": "1.0.0",
  "description": "CLI for creating Bitbucket repository",
  "main": "dist/index.js",
  "scripts": {
    "test": "jest --watch",
    "test:once": "jest",
    "clear": "rimraf dist",
    "bundle": "rollup -c",
    "build": "npm run clear && npm run bundle"
  },
  "author": "Ahmed Abbas",
  "license": "MIT",
  "devDependencies": {
    "@types/jest": "^24.0.11",
    "jest": "^24.5.0",
    "nock": "^10.0.6",
    "rimraf": "^2.6.3",
    "rollup": "^1.7.3",
    "rollup-plugin-commonjs": "^9.2.2",
    "rollup-plugin-json": "^4.0.0",
    "rollup-plugin-node-resolve": "^4.0.1"
  }
}
